apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-backup.serviceAccountName" . }}
  labels:
    {{- include "dclaw-backup.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
