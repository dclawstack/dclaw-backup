apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-backup.fullname" . }}
  labels:
    {{- include "dclaw-backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-backup.selectorLabels" . | nindent 4 }}
